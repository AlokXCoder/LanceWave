import React from 'react';
import Navbar from './Navbar';

const Header = () => {
    return (
        <div>
              <title>Navbar || Lance Wave</title>
            <Navbar></Navbar>
        </div>
    );
};

export default Header;